# Python面向对象 - 训练题目

## 学习目标
通过实践练习掌握Python面向对象编程，包括类设计、继承、多态、特殊方法和设计模式。

## 每日训练题目

### 第1周：类和对象基础

#### 题目1：类的基本定义
**难度**：★☆☆☆☆

编写程序实现以下功能：
1. 定义一个简单的类
2. 定义实例变量和类变量
3. 定义实例方法和类方法
4. 定义静态方法

**要求**：
```python
class Circle:
    """圆类"""
    # 类变量
    pi = 3.14159
    
    def __init__(self, radius):
        """初始化方法"""
        # 你的代码实现
        pass
    
    def area(self):
        """计算面积"""
        pass
    
    @classmethod
    def from_diameter(cls, diameter):
        """类方法：通过直径创建圆"""
        pass
    
    @staticmethod
    def is_valid_radius(radius):
        """静态方法：验证半径是否有效"""
        pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目2：封装和属性访问
**难度**：★★☆☆☆

编写程序实现以下功能：
1. 使用私有属性（名称改写）
2. 使用@property装饰器
3. 实现属性的getter和setter
4. 实现只读属性

**要求**：
```python
class BankAccount:
    """银行账户类"""
    
    def __init__(self, account_number, balance=0):
        """初始化账户"""
        # 你的代码实现
        # account_number应该是只读的
        # balance应该通过属性访问，并且有验证
        pass
    
    @property
    def balance(self):
        """余额属性（只读）"""
        pass
    
    @balance.setter
    def balance(self, value):
        """设置余额，需要验证"""
        pass
    
    def deposit(self, amount):
        """存款"""
        pass
    
    def withdraw(self, amount):
        """取款"""
        pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目3：继承和多态
**难度**：★★★☆☆

编写程序实现以下功能：
1. 定义父类和子类
2. 方法重写（override）
3. 使用super()调用父类方法
4. 实现多态

**要求**：
```python
class Shape:
    """图形基类"""
    
    def area(self):
        """计算面积（抽象方法）"""
        raise NotImplementedError("子类必须实现area方法")
    
    def perimeter(self):
        """计算周长（抽象方法）"""
        raise NotImplementedError("子类必须实现perimeter方法")
    
    def __str__(self):
        """字符串表示"""
        return f"{self.__class__.__name__}"

class Rectangle(Shape):
    """矩形类"""
    
    def __init__(self, width, height):
        """初始化矩形"""
        # 你的代码实现
        pass
    
    def area(self):
        """计算面积"""
        pass
    
    def perimeter(self):
        """计算周长"""
        pass

class Circle(Shape):
    """圆形类"""
    
    def __init__(self, radius):
        """初始化圆形"""
        # 你的代码实现
        pass
    
    def area(self):
        """计算面积"""
        pass
    
    def perimeter(self):
        """计算周长"""
        pass

def print_shape_info(shape):
    """多态示例：处理任何Shape子类"""
    # 你的代码实现
    pass

if __name__ == "__main__":
    # 测试代码
    shapes = [
        Rectangle(5, 3),
        Circle(4),
        Rectangle(2, 2)
    ]
    for shape in shapes:
        print_shape_info(shape)
```

---

#### 题目4：特殊方法
**难度**：★★★☆☆

编写程序实现以下功能：
1. 实现__str__和__repr__
2. 实现比较运算符（__eq__, __lt__等）
3. 实现数值运算符（__add__, __mul__等）
4. 实现容器操作（__len__, __getitem__等）

**要求**：
```python
class Vector:
    """向量类"""
    
    def __init__(self, x, y, z=0):
        """初始化向量"""
        # 你的代码实现
        pass
    
    def __str__(self):
        """字符串表示（用户友好）"""
        pass
    
    def __repr__(self):
        """字符串表示（开发者友好）"""
        pass
    
    def __eq__(self, other):
        """相等比较"""
        pass
    
    def __add__(self, other):
        """向量加法"""
        pass
    
    def __sub__(self, other):
        """向量减法"""
        pass
    
    def __mul__(self, scalar):
        """标量乘法"""
        pass
    
    def __len__(self):
        """向量长度（返回维度）"""
        pass
    
    def __getitem__(self, index):
        """索引访问"""
        pass
    
    def magnitude(self):
        """计算向量模长"""
        pass

if __name__ == "__main__":
    # 测试代码
    v1 = Vector(1, 2, 3)
    v2 = Vector(4, 5, 6)
    print(v1 + v2)
    print(v1 * 2)
    print(len(v1))
```

---

#### 题目5：抽象基类
**难度**：★★★☆☆

编写程序实现以下功能：
1. 使用abc模块定义抽象基类
2. 定义抽象方法
3. 实现抽象基类的子类
4. 使用抽象基类实现接口

**要求**：
```python
from abc import ABC, abstractmethod

class Animal(ABC):
    """动物抽象基类"""
    
    @abstractmethod
    def make_sound(self):
        """发出声音"""
        pass
    
    @abstractmethod
    def move(self):
        """移动"""
        pass
    
    def sleep(self):
        """睡觉（具体方法）"""
        print("Zzz...")

class Dog(Animal):
    """狗类"""
    
    def make_sound(self):
        """实现抽象方法"""
        pass
    
    def move(self):
        """实现抽象方法"""
        pass

class Bird(Animal):
    """鸟类"""
    
    def make_sound(self):
        """实现抽象方法"""
        pass
    
    def move(self):
        """实现抽象方法"""
        pass

if __name__ == "__main__":
    # 测试代码
    # 不能实例化抽象类
    # animal = Animal()  # 这应该报错
    dog = Dog()
    bird = Bird()
```

---

### 第2周：高级特性

#### 题目6：描述符
**难度**：★★★★☆

编写程序实现以下功能：
1. 实现描述符协议
2. 创建数据描述符
3. 使用描述符实现属性验证

**要求**：
```python
class PositiveNumber:
    """正数描述符"""
    
    def __init__(self):
        self.name = None
    
    def __get__(self, instance, owner):
        """获取值"""
        pass
    
    def __set__(self, instance, value):
        """设置值，验证为正数"""
        pass
    
    def __set_name__(self, owner, name):
        """设置属性名"""
        pass

class Rectangle:
    """矩形类，使用描述符验证"""
    width = PositiveNumber()
    height = PositiveNumber()
    
    def __init__(self, width, height):
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height

if __name__ == "__main__":
    # 测试代码
    rect = Rectangle(5, 3)
    print(rect.area())
    # rect.width = -1  # 这应该抛出异常
```

---

#### 题目7：设计模式 - 单例模式
**难度**：★★★☆☆

编写程序实现以下功能：
1. 使用__new__实现单例模式
2. 使用装饰器实现单例模式
3. 使用元类实现单例模式

**要求**：
```python
# 方法1：使用__new__
class Singleton1:
    _instance = None
    
    def __new__(cls):
        """单例实现"""
        pass
    
    def __init__(self):
        """初始化"""
        pass

# 方法2：使用装饰器
def singleton(cls):
    """单例装饰器"""
    instances = {}
    def get_instance(*args, **kwargs):
        # 你的代码实现
        pass
    return get_instance

@singleton
class Singleton2:
    def __init__(self):
        pass

# 方法3：使用元类
class SingletonMeta(type):
    """单例元类"""
    _instances = {}
    
    def __call__(cls, *args, **kwargs):
        # 你的代码实现
        pass

class Singleton3(metaclass=SingletonMeta):
    def __init__(self):
        pass

if __name__ == "__main__":
    # 测试代码
    s1 = Singleton1()
    s2 = Singleton1()
    print(s1 is s2)  # 应该是True
```

---

#### 题目8：设计模式 - 观察者模式
**难度**：★★★★☆

编写程序实现观察者模式：
1. 定义观察者接口
2. 定义被观察者（主题）
3. 实现观察者的注册和通知机制

**要求**：
```python
class Observer(ABC):
    """观察者抽象基类"""
    
    @abstractmethod
    def update(self, message):
        """更新方法"""
        pass

class Subject:
    """被观察者（主题）"""
    
    def __init__(self):
        """初始化"""
        self._observers = []
        self._state = None
    
    def attach(self, observer):
        """注册观察者"""
        pass
    
    def detach(self, observer):
        """移除观察者"""
        pass
    
    def notify(self, message):
        """通知所有观察者"""
        pass
    
    def set_state(self, state):
        """设置状态并通知观察者"""
        pass

class EmailObserver(Observer):
    """邮件观察者"""
    
    def update(self, message):
        """实现更新方法"""
        print(f"发送邮件: {message}")

class SMSObserver(Observer):
    """短信观察者"""
    
    def update(self, message):
        """实现更新方法"""
        print(f"发送短信: {message}")

if __name__ == "__main__":
    # 测试代码
    subject = Subject()
    email_obs = EmailObserver()
    sms_obs = SMSObserver()
    
    subject.attach(email_obs)
    subject.attach(sms_obs)
    
    subject.set_state("新订单")
```

---

#### 题目9：设计模式 - 工厂模式
**难度**：★★★☆☆

编写程序实现工厂模式：
1. 定义产品接口
2. 实现具体产品类
3. 实现工厂类

**要求**：
```python
class Animal(ABC):
    """动物抽象类"""
    
    @abstractmethod
    def speak(self):
        """说话"""
        pass

class Dog(Animal):
    def speak(self):
        return "Woof!"

class Cat(Animal):
    def speak(self):
        return "Meow!"

class Duck(Animal):
    def speak(self):
        return "Quack!"

class AnimalFactory:
    """动物工厂类"""
    
    @staticmethod
    def create_animal(animal_type):
        """创建动物实例"""
        # 你的代码实现
        pass

if __name__ == "__main__":
    # 测试代码
    factory = AnimalFactory()
    dog = factory.create_animal("dog")
    cat = factory.create_animal("cat")
    print(dog.speak())
    print(cat.speak())
```

---

#### 题目10：综合练习 - 图书管理系统
**难度**：★★★★☆

编写一个图书管理系统，实现以下功能：

**要求**：
```python
class Book:
    """图书类"""
    
    def __init__(self, isbn, title, author, price):
        """初始化图书"""
        # 使用描述符验证isbn和price
        pass
    
    def __str__(self):
        """字符串表示"""
        pass
    
    def __eq__(self, other):
        """比较两本书是否相同（基于ISBN）"""
        pass

class Library:
    """图书馆类"""
    
    def __init__(self, name):
        """初始化图书馆"""
        self.name = name
        self.books = {}  # 使用字典存储，键为ISBN
    
    def add_book(self, book):
        """添加图书"""
        pass
    
    def remove_book(self, isbn):
        """删除图书"""
        pass
    
    def find_book(self, isbn):
        """查找图书"""
        pass
    
    def search_by_title(self, title):
        """按标题搜索"""
        pass
    
    def search_by_author(self, author):
        """按作者搜索"""
        pass
    
    def __len__(self):
        """返回图书数量"""
        pass
    
    def __iter__(self):
        """迭代器，支持for循环"""
        pass

if __name__ == "__main__":
    # 测试代码
    library = Library("市图书馆")
    # 添加图书
    # 搜索图书
    # 遍历图书
    pass
```

---

## 项目练习

### 项目1：设计模式实践

选择3-5个设计模式，实现完整的示例：
- 单例模式
- 工厂模式
- 观察者模式
- 策略模式
- 装饰器模式

**要求**：
- 每个模式要有清晰的使用场景说明
- 代码注释完整
- 包含单元测试
- 符合SOLID原则

---

### 项目2：面向对象设计系统

设计并实现一个小型系统（如：员工管理系统、课程管理系统等）：

**要求**：
- 使用继承和多态
- 使用抽象基类定义接口
- 使用设计模式解决实际问题
- 完善的异常处理
- 代码符合PEP 8规范
- 包含设计文档（SDD）

---

## 学习检查

完成以上题目后，检查以下内容：

- [ ] 能够定义和使用类
- [ ] 理解封装、继承、多态
- [ ] 掌握特殊方法的使用
- [ ] 能够使用抽象基类
- [ ] 理解描述符协议
- [ ] 能够实现常见的设计模式
- [ ] 理解SOLID原则
- [ ] 能够进行面向对象设计
- [ ] 代码符合PEP 8规范



