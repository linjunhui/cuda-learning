# Python高级特性 - 训练题目

## 学习目标
通过实践练习掌握Python高级特性，包括装饰器、生成器、上下文管理器、元类等，能够编写更加优雅和高效的Python代码。

## 每日训练题目

### 第1周：装饰器

#### 题目1：函数装饰器基础
**难度**：★★☆☆☆

编写程序实现函数装饰器：
1. 简单的函数装饰器
2. 带参数的装饰器
3. 类装饰器

**要求**：
```python
def timer(func):
    """计时装饰器"""
    import time
    import functools
    
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        # 你的代码实现
        # 记录函数执行时间
        pass
    return wrapper

def cache(func):
    """缓存装饰器"""
    cache_dict = {}
    import functools
    
    @functools.wraps(func)
    def wrapper(*args):
        # 你的代码实现
        # 实现函数结果缓存
        pass
    return wrapper

def repeat(n):
    """重复执行装饰器（带参数）"""
    def decorator(func):
        import functools
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            # 你的代码实现
            # 重复执行函数n次
            pass
        return wrapper
    return decorator

@timer
def fibonacci(n):
    """斐波那契数列"""
    if n < 2:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

if __name__ == "__main__":
    # 测试代码
    result = fibonacci(10)
    print(result)
```

---

#### 题目2：装饰器应用
**难度**：★★★☆☆

编写程序实现实用的装饰器：
1. 日志记录装饰器
2. 权限检查装饰器
3. 重试装饰器

**要求**：
```python
import functools
import time

def log(func):
    """日志记录装饰器"""
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        # 你的代码实现
        # 记录函数调用信息
        pass
    return wrapper

def require_role(role):
    """权限检查装饰器（带参数）"""
    def decorator(func):
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            # 假设有一个current_user变量
            # 检查用户角色
            # 你的代码实现
            pass
        return wrapper
    return decorator

def retry(max_attempts=3, delay=1):
    """重试装饰器"""
    def decorator(func):
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            # 你的代码实现
            # 函数失败时重试
            pass
        return wrapper
    return decorator

@log
def process_data(data):
    """处理数据"""
    # 模拟处理
    pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

### 第2周：生成器和迭代器

#### 题目3：迭代器实现
**难度**：★★★☆☆

编写程序实现迭代器：
1. 实现迭代器协议
2. 自定义迭代器类
3. 使用迭代器

**要求**：
```python
class CountDown:
    """倒计时迭代器"""
    
    def __init__(self, start):
        """初始化"""
        self.current = start
    
    def __iter__(self):
        """返回迭代器"""
        return self
    
    def __next__(self):
        """返回下一个值"""
        # 你的代码实现
        pass

class FibonacciIterator:
    """斐波那契数列迭代器"""
    
    def __init__(self, n):
        """生成前n个斐波那契数"""
        # 你的代码实现
        pass
    
    def __iter__(self):
        return self
    
    def __next__(self):
        # 你的代码实现
        pass

if __name__ == "__main__":
    # 测试代码
    for num in CountDown(5):
        print(num)
    
    fib = FibonacciIterator(10)
    for num in fib:
        print(num)
```

---

#### 题目4：生成器实现
**难度**：★★★☆☆

编写程序实现生成器：
1. 生成器函数
2. 生成器表达式
3. 生成器的方法（send, throw, close）

**要求**：
```python
def fibonacci_generator():
    """斐波那契数列生成器"""
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b

def prime_generator(limit):
    """质数生成器"""
    # 你的代码实现
    # 生成小于limit的所有质数
    pass

def file_reader(filename):
    """文件读取生成器"""
    # 你的代码实现
    # 逐行读取文件
    pass

def generator_with_send():
    """使用send方法的生成器"""
    value = yield "First"
    while True:
        value = yield f"Received: {value}"

if __name__ == "__main__":
    # 测试代码
    fib = fibonacci_generator()
    for i in range(10):
        print(next(fib))
    
    # 测试send
    gen = generator_with_send()
    print(next(gen))
    print(gen.send("Hello"))
    print(gen.send("World"))
```

---

#### 题目5：生成器应用
**难度**：★★★☆☆

编写程序使用生成器解决实际问题：
1. 数据管道处理
2. 无限序列生成
3. 内存优化的数据处理

**要求**：
```python
def number_generator(start, end):
    """数字生成器"""
    # 你的代码实现
    pass

def square_generator(gen):
    """平方生成器"""
    # 你的代码实现
    # 对生成器中的每个数求平方
    pass

def filter_even_generator(gen):
    """过滤偶数生成器"""
    # 你的代码实现
    pass

def data_pipeline(data_source):
    """数据管道处理"""
    # 你的代码实现
    # 组合多个生成器形成处理管道
    pass

def infinite_counter(start=0, step=1):
    """无限计数器"""
    # 你的代码实现
    pass

if __name__ == "__main__":
    # 测试代码
    numbers = number_generator(1, 10)
    squares = square_generator(numbers)
    evens = filter_even_generator(squares)
    for num in evens:
        print(num)
```

---

### 第3周：上下文管理器

#### 题目6：上下文管理器实现
**难度**：★★★☆☆

编写程序实现上下文管理器：
1. 使用类实现上下文管理器
2. 使用@contextmanager装饰器
3. 嵌套上下文管理器

**要求**：
```python
from contextlib import contextmanager

class FileHandler:
    """文件处理上下文管理器"""
    
    def __init__(self, filename, mode):
        """初始化"""
        self.filename = filename
        self.mode = mode
        self.file = None
    
    def __enter__(self):
        """进入上下文"""
        # 你的代码实现
        pass
    
    def __exit__(self, exc_type, exc_val, exc_tb):
        """退出上下文"""
        # 你的代码实现
        # 处理异常
        pass

@contextmanager
def timer():
    """计时上下文管理器"""
    # 你的代码实现
    pass

@contextmanager
def suppress_exception(*exceptions):
    """抑制异常的上下文管理器"""
    # 你的代码实现
    # 抑制指定的异常类型
    pass

class DatabaseConnection:
    """数据库连接上下文管理器"""
    
    def __enter__(self):
        """建立连接"""
        # 你的代码实现
        pass
    
    def __exit__(self, exc_type, exc_val, exc_tb):
        """关闭连接"""
        # 你的代码实现
        pass
    
    def execute(self, query):
        """执行查询"""
        pass

if __name__ == "__main__":
    # 测试代码
    with FileHandler("test.txt", "w") as f:
        f.write("Hello, World!")
    
    with timer():
        # 一些耗时操作
        pass
```

---

#### 题目7：上下文管理器应用
**难度**：★★★☆☆

编写程序使用上下文管理器解决实际问题：
1. 锁的上下文管理器
2. 临时目录的上下文管理器
3. 数据库事务的上下文管理器

**要求**：
```python
import threading
from contextlib import contextmanager
import tempfile
import shutil

class Lock:
    """锁的上下文管理器"""
    
    def __init__(self):
        """初始化锁"""
        self.lock = threading.Lock()
    
    def __enter__(self):
        """获取锁"""
        # 你的代码实现
        pass
    
    def __exit__(self, exc_type, exc_val, exc_tb):
        """释放锁"""
        # 你的代码实现
        pass

@contextmanager
def temp_directory():
    """临时目录上下文管理器"""
    # 你的代码实现
    # 创建临时目录，退出时删除
    pass

class Transaction:
    """数据库事务上下文管理器"""
    
    def __init__(self, connection):
        """初始化事务"""
        self.connection = connection
        self.committed = False
    
    def __enter__(self):
        """开始事务"""
        # 你的代码实现
        pass
    
    def __exit__(self, exc_type, exc_val, exc_tb):
        """提交或回滚事务"""
        # 你的代码实现
        # 如果没有异常，提交；否则回滚
        pass
    
    def commit(self):
        """提交事务"""
        pass
    
    def rollback(self):
        """回滚事务"""
        pass

if __name__ == "__main__":
    # 测试代码
    lock = Lock()
    with lock:
        # 临界区代码
        pass
    
    with temp_directory() as temp_dir:
        # 使用临时目录
        pass
```

---

### 第4周：元类和元编程

#### 题目8：描述符深入
**难度**：★★★★☆

编写程序实现描述符的高级应用：
1. 缓存描述符
2. 验证描述符
3. 延迟加载描述符

**要求**：
```python
class CachedProperty:
    """缓存属性描述符"""
    
    def __init__(self, func):
        """初始化"""
        self.func = func
        self.name = None
    
    def __set_name__(self, owner, name):
        """设置属性名"""
        self.name = name
    
    def __get__(self, instance, owner):
        """获取属性值（带缓存）"""
        # 你的代码实现
        # 第一次访问时计算并缓存
        # 后续访问直接返回缓存值
        pass

class ValidatedProperty:
    """验证属性描述符"""
    
    def __init__(self, validator):
        """初始化，validator是验证函数"""
        self.validator = validator
        self.name = None
    
    def __set_name__(self, owner, name):
        self.name = name
    
    def __get__(self, instance, owner):
        """获取属性值"""
        pass
    
    def __set__(self, instance, value):
        """设置属性值（带验证）"""
        # 你的代码实现
        # 使用validator验证值
        pass

class LazyProperty:
    """延迟加载属性描述符"""
    
    def __init__(self, func):
        """初始化"""
        self.func = func
        self.name = None
    
    def __set_name__(self, owner, name):
        self.name = name
    
    def __get__(self, instance, owner):
        """延迟加载属性"""
        # 你的代码实现
        # 第一次访问时执行func
        pass

if __name__ == "__main__":
    # 测试代码
    class Data:
        @CachedProperty
        def expensive_computation(self):
            print("Computing...")
            return sum(range(1000000))
    
    data = Data()
    print(data.expensive_computation)  # 第一次计算
    print(data.expensive_computation)  # 使用缓存
```

---

#### 题目9：元类基础
**难度**：★★★★☆

编写程序使用元类：
1. 理解type类
2. 创建简单的元类
3. 使用元类控制类创建

**要求**：
```python
def create_class_with_meta():
    """使用type动态创建类"""
    # 使用type(name, bases, dict)创建类
    # 你的代码实现
    pass

class SingletonMeta(type):
    """单例元类"""
    _instances = {}
    
    def __call__(cls, *args, **kwargs):
        """控制实例创建"""
        # 你的代码实现
        # 确保只有一个实例
        pass

class AutoRegisterMeta(type):
    """自动注册元类"""
    registry = {}
    
    def __new__(mcs, name, bases, namespace):
        """创建类时自动注册"""
        # 你的代码实现
        # 将类注册到registry中
        pass

class MyClass(metaclass=SingletonMeta):
    """使用单例元类的类"""
    def __init__(self):
        self.value = 42

if __name__ == "__main__":
    # 测试代码
    obj1 = MyClass()
    obj2 = MyClass()
    print(obj1 is obj2)  # 应该是True
```

---

#### 题目10：元类应用
**难度`：★★★★★

编写程序使用元类解决实际问题：
1. 自动添加方法
2. 属性验证框架
3. ORM基础框架

**要求**：
```python
class AutoPropertyMeta(type):
    """自动添加属性的元类"""
    
    def __new__(mcs, name, bases, namespace):
        """自动添加属性访问方法"""
        # 你的代码实现
        # 为类自动添加getter和setter方法
        pass

class ValidatedMeta(type):
    """验证框架元类"""
    
    def __new__(mcs, name, bases, namespace):
        """自动添加验证逻辑"""
        # 你的代码实现
        # 根据类型注解自动添加验证
        pass

class Field:
    """字段描述符"""
    def __init__(self, field_type):
        self.field_type = field_type
        self.name = None
    
    def __set_name__(self, owner, name):
        self.name = name
    
    def __get__(self, instance, owner):
        pass
    
    def __set__(self, instance, value):
        # 类型验证
        pass

class ModelMeta(type):
    """ORM模型元类"""
    
    def __new__(mcs, name, bases, namespace):
        """创建模型类"""
        # 你的代码实现
        # 收集字段信息
        # 生成SQL方法等
        pass

class Model(metaclass=ModelMeta):
    """ORM模型基类"""
    pass

class User(Model):
    """用户模型（示例）"""
    name = Field(str)
    age = Field(int)

if __name__ == "__main__":
    # 测试代码
    user = User()
    user.name = "Alice"
    user.age = 25
    # user.age = "25"  # 应该抛出类型错误
```

---

## 项目练习

### 项目1：装饰器工具库

实现一个装饰器工具库，包含：
- 性能分析装饰器
- 缓存装饰器
- 日志装饰器
- 重试装饰器
- 权限检查装饰器

**要求**：
- 完整的API文档
- 使用示例
- 单元测试
- 代码覆盖率≥80%

---

### 项目2：数据处理管道

使用生成器实现数据处理管道：
- 数据读取生成器
- 数据转换生成器
- 数据过滤生成器
- 数据聚合生成器
- 组合形成完整管道

**要求**：
- 内存高效
- 可组合的设计
- 完善的文档
- 性能测试

---

### 项目3：ORM框架基础

使用元类实现一个简单的ORM框架：
- 模型定义
- 字段类型
- 查询接口
- 数据库连接管理

**要求**：
- 使用元类实现
- 类型验证
- 基本的CRUD操作
- 设计文档（SDD）
- 代码覆盖率≥80%

---

## 学习检查

完成以上题目后，检查以下内容：

- [ ] 能够编写和使用装饰器
- [ ] 理解生成器和迭代器
- [ ] 能够实现上下文管理器
- [ ] 理解描述符协议
- [ ] 理解元类的概念和使用
- [ ] 能够使用元类解决问题
- [ ] 代码符合PEP 8规范
- [ ] 能够编写优雅的Python代码



