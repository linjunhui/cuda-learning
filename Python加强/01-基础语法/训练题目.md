# Python基础语法 - 训练题目

## 学习目标
通过实践练习掌握Python基本语法和特性，包括变量类型、控制流、函数、数据结构、文件操作和异常处理。

## 每日训练题目

### 第1周：Python核心语法

#### 题目1：变量和数据类型
**难度**：★☆☆☆☆

编写程序实现以下功能：
1. 声明不同类型的变量（int, float, str, bool）
2. 进行类型转换
3. 使用type()函数检查类型
4. 使用isinstance()函数进行类型检查

**要求**：
```python
# 你的代码实现
# 1. 声明变量
# 2. 类型转换示例
# 3. 类型检查示例

# 测试代码
if __name__ == "__main__":
    # 在这里测试你的代码
    pass
```

**提示**：
- 整数：`x = 42`
- 浮点数：`y = 3.14`
- 字符串：`z = "hello"`
- 布尔值：`flag = True`

---

#### 题目2：控制流语句
**难度**：★☆☆☆☆

编写程序实现以下功能：
1. 使用if-elif-else判断成绩等级（A/B/C/D/F）
2. 使用for循环计算1到100的和
3. 使用while循环实现猜数字游戏
4. 使用break和continue控制循环

**要求**：
```python
def grade_classifier(score):
    """根据分数返回等级"""
    # 你的代码实现
    # 90-100: A, 80-89: B, 70-79: C, 60-69: D, <60: F
    pass

def sum_numbers(n):
    """计算1到n的和"""
    # 使用for循环实现
    pass

def guess_number_game():
    """猜数字游戏"""
    # 随机生成一个1-100的数字，让用户猜
    # 使用while循环
    pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目3：函数定义和调用
**难度**：★★☆☆☆

编写程序实现以下功能：
1. 定义带默认参数的函数
2. 定义使用*args的函数
3. 定义使用**kwargs的函数
4. 定义lambda函数
5. 使用docstring编写函数文档

**要求**：
```python
def greet(name, greeting="Hello", *args, **kwargs):
    """
    问候函数
    
    参数:
        name: 姓名
        greeting: 问候语，默认为"Hello"
        *args: 额外的位置参数
        **kwargs: 额外的关键字参数
    
    返回:
        问候字符串
    """
    # 你的代码实现
    pass

def calculate_average(*numbers):
    """计算多个数的平均值"""
    # 你的代码实现
    pass

def create_person(name, age, **info):
    """创建人员信息字典"""
    # 你的代码实现，返回包含所有信息的字典
    pass

# Lambda函数示例
square = lambda x: x ** 2
# 实现更多lambda函数

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目4：列表操作
**难度**：★★☆☆☆

编写程序实现以下功能：
1. 列表的创建、添加、删除、修改
2. 列表推导式
3. 列表切片操作
4. 列表常用方法（append, extend, insert, remove, pop, sort等）

**要求**：
```python
def list_basic_operations():
    """列表基本操作"""
    # 创建列表
    # 添加元素（append, extend, insert）
    # 删除元素（remove, pop, del）
    # 修改元素
    pass

def list_comprehension():
    """列表推导式练习"""
    # 1. 生成1-10的平方列表
    # 2. 生成1-10中偶数的平方列表
    # 3. 生成嵌套列表的扁平化
    pass

def list_slicing():
    """列表切片操作"""
    numbers = list(range(20))
    # 1. 获取前5个元素
    # 2. 获取后5个元素
    # 3. 获取中间的元素
    # 4. 反转列表
    # 5. 每隔2个取一个元素
    pass

def list_methods():
    """列表方法练习"""
    # 测试各种列表方法
    pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目5：字典操作
**难度**：★★☆☆☆

编写程序实现以下功能：
1. 字典的创建、添加、删除、修改
2. 字典推导式
3. 字典常用方法（keys, values, items, get, update等）
4. 字典的遍历

**要求**：
```python
def dict_basic_operations():
    """字典基本操作"""
    # 创建字典（多种方式）
    # 添加和修改键值对
    # 删除键值对
    # 访问值
    pass

def dict_comprehension():
    """字典推导式练习"""
    # 1. 将两个列表合并为字典
    # 2. 将字典的值平方
    # 3. 过滤字典（只保留值大于10的项）
    pass

def dict_methods():
    """字典方法练习"""
    student = {"name": "Alice", "age": 20, "grade": "A"}
    # 使用keys(), values(), items()
    # 使用get()方法安全访问
    # 使用update()方法合并字典
    # 使用setdefault()方法
    pass

def count_characters(text):
    """统计文本中每个字符的出现次数"""
    # 使用字典实现
    # 返回字符计数字典
    pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目6：集合操作
**难度**：★★☆☆☆

编写程序实现以下功能：
1. 集合的创建和基本操作
2. 集合运算（交集、并集、差集、对称差集）
3. 集合推导式
4. 集合常用方法

**要求**：
```python
def set_basic_operations():
    """集合基本操作"""
    # 创建集合
    # 添加元素（add）
    # 删除元素（remove, discard, pop）
    # 集合运算
    pass

def set_operations():
    """集合运算练习"""
    set1 = {1, 2, 3, 4, 5}
    set2 = {4, 5, 6, 7, 8}
    # 交集: &
    # 并集: |
    # 差集: -
    # 对称差集: ^
    pass

def find_unique_elements(list1, list2):
    """找出两个列表中只在一个列表中出现的元素"""
    # 使用集合实现
    pass

def set_comprehension():
    """集合推导式练习"""
    # 生成1-20中能被3整除的数的集合
    pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目7：文件操作
**难度**：★★★☆☆

编写程序实现以下功能：
1. 读取文本文件
2. 写入文本文件
3. 使用with语句安全操作文件
4. 文件指针操作（seek, tell）
5. 处理不同编码的文件

**要求**：
```python
def read_file(filename):
    """读取文件内容"""
    # 使用open()和with语句
    # 返回文件内容
    pass

def write_file(filename, content):
    """写入文件"""
    # 使用with语句
    pass

def copy_file(src, dst):
    """复制文件"""
    # 读取源文件并写入目标文件
    pass

def process_log_file(filename):
    """处理日志文件"""
    # 读取文件，统计行数、单词数、字符数
    # 返回统计结果字典
    pass

def append_to_file(filename, content):
    """追加内容到文件"""
    # 使用追加模式
    pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目8：异常处理
**难度**：★★★☆☆

编写程序实现以下功能：
1. 使用try-except捕获异常
2. 捕获多个异常类型
3. 使用else和finally子句
4. 抛出异常（raise）
5. 自定义异常类

**要求**：
```python
def safe_divide(a, b):
    """安全除法，处理除零异常"""
    # 使用try-except
    pass

def read_user_input():
    """读取用户输入，处理异常"""
    # 处理ValueError等异常
    pass

def file_operations_with_exception(filename):
    """文件操作，处理FileNotFoundError等异常"""
    # 使用try-except-finally
    pass

class CustomError(Exception):
    """自定义异常类"""
    pass

def validate_age(age):
    """验证年龄，如果无效抛出自定义异常"""
    # 如果年龄小于0或大于150，抛出CustomError
    pass

def process_data(data):
    """处理数据，包含多层异常处理"""
    # 使用多个except子句
    pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目9：模块和包
**难度**：★★★☆☆

编写程序实现以下功能：
1. 创建自定义模块
2. 导入和使用模块
3. 创建包结构
4. 使用标准库模块（os, sys, datetime, json等）

**要求**：
```python
# 创建math_utils.py模块
# 包含以下函数：

def add(a, b):
    """加法"""
    pass

def multiply(a, b):
    """乘法"""
    pass

def power(base, exponent):
    """幂运算"""
    pass

# 创建main.py使用模块
# import math_utils
# 使用math_utils中的函数

# 练习使用标准库
def use_os_module():
    """使用os模块"""
    # 获取当前目录
    # 列出目录内容
    # 创建目录等
    pass

def use_datetime_module():
    """使用datetime模块"""
    # 获取当前时间
    # 格式化时间
    # 计算时间差
    pass

def use_json_module():
    """使用json模块"""
    # 将字典转换为JSON字符串
    # 将JSON字符串转换为字典
    # 读写JSON文件
    pass

if __name__ == "__main__":
    # 测试代码
    pass
```

---

#### 题目10：综合练习
**难度**：★★★☆☆

编写一个简单的学生成绩管理系统：

**要求**：
```python
class StudentManager:
    """
    学生成绩管理系统
    
    功能：
    1. 添加学生信息（姓名、学号、成绩）
    2. 删除学生信息
    3. 查询学生信息
    4. 计算平均成绩
    5. 保存到文件
    6. 从文件加载
    """
    
    def __init__(self):
        """初始化"""
        self.students = {}  # 使用字典存储，键为学号
    
    def add_student(self, student_id, name, score):
        """添加学生"""
        # 你的代码实现
        # 需要验证输入（学号格式、成绩范围等）
        # 处理异常情况
        pass
    
    def remove_student(self, student_id):
        """删除学生"""
        pass
    
    def get_student(self, student_id):
        """查询学生"""
        pass
    
    def calculate_average(self):
        """计算平均成绩"""
        pass
    
    def save_to_file(self, filename):
        """保存到文件"""
        # 使用JSON格式保存
        pass
    
    def load_from_file(self, filename):
        """从文件加载"""
        # 处理文件不存在等异常
        pass
    
    def display_all(self):
        """显示所有学生信息"""
        pass

if __name__ == "__main__":
    # 测试代码
    manager = StudentManager()
    # 添加测试数据
    # 测试各种功能
    # 测试文件保存和加载
    pass
```

---

## 项目练习

### 项目1：数据处理脚本

编写一个数据处理脚本，实现以下功能：
1. 读取CSV文件（可以使用Python内置的csv模块）
2. 数据清洗（去除空值、格式化等）
3. 数据统计（计数、求和、平均值等）
4. 结果输出到文件

**要求**：
- 使用函数式编程思想
- 完善的异常处理
- 使用列表推导式和字典推导式
- 代码符合PEP 8规范

---

### 项目2：文件管理工具

编写一个文件管理工具，实现以下功能：
1. 列出指定目录下的所有文件
2. 按文件类型分类
3. 统计文件大小
4. 查找特定类型的文件
5. 批量重命名文件

**要求**：
- 使用os和pathlib模块
- 使用异常处理确保程序稳定
- 提供命令行界面
- 使用上下文管理器

---

## 学习检查

完成以上题目后，检查以下内容：

- [ ] 能够熟练使用Python基本数据类型
- [ ] 掌握控制流语句的使用
- [ ] 能够定义和调用函数
- [ ] 熟练操作列表、字典、集合
- [ ] 能够进行文件操作
- [ ] 掌握异常处理机制
- [ ] 能够创建和使用模块
- [ ] 代码符合PEP 8规范
- [ ] 能够编写清晰的文档字符串

